apiVersion: daisy.com/v1
kind: DaisyInstallation
metadata:
  name: test-001
spec:
  pvReclaimPolicy: Retain
  configuration:
    settings:
      listen_host: "0.0.0.0"
    clusters:
      cluster:
        name: cluster
        layout:
          shardsCount: 1
          replicasCount: 1
