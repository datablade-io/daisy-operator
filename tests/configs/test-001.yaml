apiVersion: daisy.com/v1
kind: DaisyInstallation
metadata:
  name: test-001
spec:
  pvReclaimPolicy: Retain
  configuration:
    clusters:
      cluster:
        name: cluster
        layout:
          shardsCount: 1
          replicasCount: 1
